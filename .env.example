# Fix It Now — Frontend environment variables
# Copy this file to .env and fill in your values

# Backend API base URL (no trailing slash)
# Default: http://localhost:4000/api/v1
VITE_API_URL=http://localhost:4000/api/v1
